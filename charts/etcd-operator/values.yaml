# Default values for etcd-operator.

crds:
  # -- Render the CRDs as part of the release. Templated (not Helm's install-only
  # crds/ dir) so `helm upgrade` keeps them current with the API types.
  enabled: true
  # -- Annotate CRDs with helm.sh/resource-policy: keep so `helm uninstall` leaves
  # them in place. Strongly recommended: deleting the CRDs cascade-deletes every
  # EtcdCluster and its data.
  keep: true

# Deny DELETE on EtcdMember objects at the API boundary, except for the
# operator itself, the garbage collector (so a deleted EtcdCluster still
# cascades) and the namespace controller (so namespace deletion does not hang).
#
# Deleting an EtcdMember by hand is not a recoverable mistake: its data PVC is
# controller-owned by the member, so the volume goes with it, while the
# member's finalizer removes the member from etcd on the way out. The docs have
# always said these objects are the operator's; this enforces it.
#
# Break-glass without uninstalling: annotate the member with
# etcd-operator.cozystack.io/allow-deletion=true, then delete it.
#
# Requires Kubernetes 1.30+ (ValidatingAdmissionPolicy GA). Set to false on
# older apiservers.
#
# Scope: with manager.watchNamespaces set, the binding is scoped to those
# namespaces so scoped releases coexist. Cluster-wide (watchNamespaces empty)
# the install must be a singleton — a second cluster-wide release would deny
# this operator's own scale-down/replacement (admission is deny-wins).
memberDeletionProtection:
  # -- Install the ValidatingAdmissionPolicy protecting EtcdMember objects.
  enabled: true
  # -- Extra usernames allowed to delete EtcdMembers, in apiserver form
  # (e.g. "system:serviceaccount:<ns>:<sa>" or a user name). For platforms
  # whose own controllers legitimately reap these objects.
  additionalAllowedUsers: []

# -- Render a Namespace object. Off by default (real `helm install` uses
# --create-namespace); build-dist-manifests turns it on so the rendered
# kubectl-apply manifest is self-contained.
namespace:
  create: false

metrics:
  serviceMonitor:
    # -- Create a prometheus-operator ServiceMonitor for the metrics endpoint.
    # Requires the monitoring.coreos.com CRDs and kubeRbacProxy.enabled.
    enabled: false

image:
  # -- Operator image repository. The published image is ghcr.io/cozystack/etcd-operator.
  repository: ghcr.io/cozystack/etcd-operator
  # -- Image tag. Defaults to the chart's appVersion (set to the release tag by the pipeline).
  tag: ""
  # -- Image pull policy.
  pullPolicy: IfNotPresent

# Operator-wide default for the etcd image that runs in member Pods (NOT the
# operator's own image above). Set the repository here to point every cluster
# at an air-gapped mirror once. Per-cluster pull credentials go on an
# EtcdCluster's spec.imagePullSecrets (a Secret in the cluster's own
# namespace), not here.
etcdImage:
  # -- Default etcd image repository (registry host + path, no tag) for member
  # Pods. Repoint at an air-gapped mirror, e.g. registry.internal/mirror/etcd.
  # The chart always wires this into the operator's ETCD_IMAGE_REPOSITORY.
  #
  # Only the repository is configurable; the tag is always derived from each
  # cluster's spec.version as "v<version>" (e.g. v3.6.11). There is no
  # per-cluster repository/tag override — the operator keys all
  # version-dependent behaviour off spec.version, so a separate tag could
  # silently disagree with it.
  #
  # Keep this in sync with the controllers.EtcdImage constant — the operator's
  # built-in fallback used only when this env is unset (outside the chart). The
  # chart always sets ETCD_IMAGE_REPOSITORY, so a drift between the two is
  # harmless here, but bump both together to keep the no-chart default honest.
  repository: quay.io/coreos/etcd

# -- Number of operator replicas (leader election picks the active one).
replicaCount: 1

# -- Image pull secrets for private registries.
imagePullSecrets: []

# -- Override the chart name portion of resource names.
nameOverride: ""
# -- Override the full resource-name prefix.
fullnameOverride: ""

serviceAccount:
  # -- Create the operator ServiceAccount.
  create: true
  # -- Name of an existing ServiceAccount to use when create is false. Required
  # in that case — the operator's ClusterRole is bound to this name.
  name: ""
  # -- Extra annotations for the ServiceAccount.
  annotations: {}

# -- Extra annotations for the operator Pod.
podAnnotations: {}
# -- Extra labels for the operator Pod.
podLabels: {}
# -- Node selector for the operator Pod.
nodeSelector: {}
# -- Tolerations for the operator Pod.
tolerations: []
# -- Affinity for the operator Pod.
affinity: {}

manager:
  # -- Namespaces the manager watches (WATCH_NAMESPACE env var). Empty = all.
  watchNamespaces: []
  # -- Resource requests/limits for the manager container.
  resources:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 10m
      memory: 64Mi

# kube-rbac-proxy fronts the manager's /metrics endpoint with SubjectAccessReview
# authz. Disable to expose metrics without the proxy (the manager then binds
# metrics on :8080 directly).
kubeRbacProxy:
  # -- Run the kube-rbac-proxy metrics sidecar.
  enabled: true
  image:
    # -- kube-rbac-proxy image repository (upstream's GitHub org; the old gcr.io/kubebuilder one is gone).
    repository: ghcr.io/kube-rbac-proxy/kube-rbac-proxy
    # -- kube-rbac-proxy image tag.
    tag: v0.22.0
    # -- kube-rbac-proxy image pull policy.
    pullPolicy: IfNotPresent
  # -- Resource requests/limits for the proxy container.
  resources:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 5m
      memory: 64Mi

metricsService:
  # -- Port the metrics Service exposes (proxy https port).
  port: 8443
